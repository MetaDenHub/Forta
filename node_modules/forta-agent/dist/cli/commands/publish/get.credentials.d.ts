/// <reference types="node" />
import fs from 'fs';
import prompts from "prompts";
import { GetKeyfile } from '../../utils/get.keyfile';
import { ListKeyfiles } from '../../utils/list.keyfiles';
export declare type GetCredentials = () => Promise<{
    publicKey: string;
    privateKey: string;
}>;
export default function provideGetCredentials(prompt: typeof prompts, filesystem: typeof fs, listKeyfiles: ListKeyfiles, getKeyfile: GetKeyfile, fortaKeystore: string, keyfileName?: string): GetCredentials;
